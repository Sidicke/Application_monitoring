runtime: python
buildCommand: "pip install -r requirements.txt"
startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
envVars:
  - key: PYTHON_VERSION
    value: 3.12.3
  - key: JWT_SECRET_KEY
    generateValue: true
  - key: DATABASE_URL
    sync: false
